# V2 工作记录（2026-09-13）

## 本轮修复与验证

1. 修复结构缺口检测器的 PostgreSQL JSON 数组表达式：跨区域系统对检测改用 `jsonb_build_array`，避免无效的 Python 风格数组语法。
2. 修复结构综合持久化对 LLM 返回空字段的安全字符串化，避免 `None`/切片和空数组参数造成单候选事务失败。
3. 修复 `rebuild_canonical.py`：
   - 补入实体 Scope Gate 的 `gate_entity` 导入；
   - 检索图节点写入使用资源已保存的 `lightrag_doc_id`，不再引用未定义变量。
4. 修复实体消歧审计方法字段：同名、别名、嵌入三条 LLM 裁决路径分别持久化为 `exact+llm`、`alias+llm`、`embedding+llm`，符合 `entity_resolutions.method` 的运行时枚举约束。
5. 修复主张准入的独立性统计：普通 PostgreSQL 游标返回元组，改用位置索引读取聚合列。
6. 回填 12 条已准入事件的已有 `EXACT/NORMALIZED` 事件证据关联。回填只使用事件自身已保存的 resource、chunk、quote 和匹配状态；未创建或改写任何事实证据。

## 定向流动采集核查

- `flow_targeted` 批次：21 条资源，其中 12 条通过范围门控并已完成 canonical 重建，9 条无关资源保持 `REJECTED`。
- 该批次的两个最终 ADMITTED 主张均为“爱晚亭位于岳麓山”，来源为长沙旅游攻略；与长沙米市或跨区域流动无关，未被用于流动综合。
- 再运行四个流动种子：川盐济楚、汉口茶市、长沙米市均因跨文档的已准入引文不足而 `SKIP_INSUFFICIENT_EVIDENCE`；徽商沿江贸易因目的地字段无证据而 `REJECT`。
- 因此 `cultural_flows=0` 保持不变。这是流动 origin/destination/content 硬门禁的正确结果，未以检索命中、旅游文本或不完整路线替代证据。

## 验收结果

- 全库独立验收第一次发现 13 条无法重定位的 ADMITTED 主张，已按规则自动 `REVOKED`；发现 12 条事件证据关联缺失。
- 修复后第二次独立验收：
  - ADMITTED 证据重定位率：`1.0`；
  - 溯源 source 存在率：`1.0`；
  - 事件无证据数：`0`；
  - 无效谓词、domain/range 越界、未解析端点、UNKNOWN 实体、任务假完成：均为 `0`。
- canonical_v2 验证和本体验证：均 `PASS`。

## 当前实时盘点

- 资源：4,034
- 未合并实体：27,625
- ADMITTED 主张：4,383
- 已验证 evidence：4,822
- 文化传统 / 过程 / 流动：6 / 6 / 0
- 开放结构缺口 / 已解决缺口：527 / 3
- 常驻增长守护进程：heartbeat 记录为第 39 周期、`RUNNING_CYCLE`。

## 交付状态

`V2_FINAL_VALIDATION.md` 的最终状态为 **FAIL**。原因是 §120 的整体完成条件尚不满足：流动证据仍为零，Golden Ten 仅部分完成，数字人文基准仍为 proxy evaluation，burn-in 为 partial gain，增长守护进程仍是运行中的 cycle mode。此结论不以代码完成、数据库有记录或局部门禁通过替代最终验收。
